<%= form_for @task do |f| %>
	<%= f.label :description %>
	<%= f.text_field :description %>
	<%= f.label :priority_number %>
	<%= f.text_field :priority_number %>
	<%= f.submit "Add to list" %>
	<!-- <%= f.submit "Add to list \"#{List.find_by_id(@task.list_id)}\""%> -->
<% end %>