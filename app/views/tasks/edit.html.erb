<%= form_for [@list,@task] do |f| %>
	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.label :priority_number %>
	<%= f.text_field :priority_number %>
	<%= f.hidden_field :list_id, :value => @list.id %>
	<%= f.submit "Add task to list" %>
<% end %>